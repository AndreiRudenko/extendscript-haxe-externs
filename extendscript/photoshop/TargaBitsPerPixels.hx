package extendscript.photoshop;

/* The resolution to use when saving an image in Targa format. */
@:native("TargaBitsPerPixels") extern enum TargaBitsPerPixels {
	
	/* 16 bits per pixel. */
	SIXTEEN; 

	/* 24 bits per pixel. */
	TWENTYFOUR; 

	/* 32 bits per pixel. */
	THIRTYTWO; 


}