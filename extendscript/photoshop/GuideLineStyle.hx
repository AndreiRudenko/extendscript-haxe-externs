package extendscript.photoshop;

/* The line style for nonprinting guides displayed over images. */
@:native("GuideLineStyle") extern enum GuideLineStyle {
	
	/* Solid. */
	SOLID; 

	/* Dashed. */
	DASHED; 


}