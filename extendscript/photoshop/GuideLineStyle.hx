package extendscript.photoshop;

/* The line style for nonprinting guides displayed over images. */
@:native("GuideLineStyle") @:enum extern enum abstract GuideLineStyle(Int) { 
	
	/* Solid. */
	var SOLID;
	
	/* Dashed. */
	var DASHED;
	
	
}
