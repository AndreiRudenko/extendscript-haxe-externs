package extendscript.photoshop;

/* The type of wave. */
@:native("WaveType") @:enum extern enum abstract WaveType(Int) { 
	
	/* Rolling. */
	var SINE;
	
	/* Triangular. */
	var TRIANGULAR;
	
	/* Square. */
	var SQUARE;
	
	
}
