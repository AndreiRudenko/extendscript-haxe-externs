package extendscript.illustrator;

/* The stroke cap. */
@:native("StrokeCap") @:enum extern enum abstract StrokeCap(Int) { 
	
	/* Butted cap. */
	var BUTTENDCAP;
	
	/* Rounded cap. */
	var ROUNDENDCAP;
	
	/* Projecting cap. */
	var PROJECTINGENDCAP;
	
	
}
