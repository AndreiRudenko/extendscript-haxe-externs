package extendscript.illustrator;

/* The version of the SVG DTD. */
@:native("SVGDTDVersion") @:enum extern enum abstract SVGDTDVersion(Int) { 
	
	/* SVG 1.0. */
	var SVG1_0;
	
	/* SVG 1.1. */
	var SVG1_1;
	
	/* SVG Tiny 1.1. */
	var SVGTINY1_1;
	
	/* SVG Tiny 1.1 Plus. */
	var SVGTINY1_1PLUS;
	
	/* SVG Basic 1.1. */
	var SVGBASIC1_1;
	
	/* SVG Tiny 1.2. */
	var SVGTINY1_2;
	
	
}
