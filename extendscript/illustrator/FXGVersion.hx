package extendscript.illustrator;

/* The FXG file format version. */
@:native("FXGVersion") @:enum extern enum abstract FXGVersion(Int) { 
	
	/* FXG version 1.0. */
	var VERSION1PT0;
	
	/* FXG version 2.0. */
	var VERSION2PT0;
	
	
}
