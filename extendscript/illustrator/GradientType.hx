package extendscript.illustrator;

/* The gradient type. */
@:native("GradientType") @:enum extern enum abstract GradientType(Int) { 
	
	/* Linear gradient. */
	var LINEAR;
	
	/* */
	var RADIAL;
	
	
}
