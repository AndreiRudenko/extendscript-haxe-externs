package extendscript.illustrator;

/* The crop style. */
@:native("CropOptions") @:enum extern enum abstract CropOptions(Int) { 
	
	/* Standard crop style. */
	var Standard;
	
	/* Japanese crop style. */
	var Japanese;
	
	
}
