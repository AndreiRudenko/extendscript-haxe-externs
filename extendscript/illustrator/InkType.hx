package extendscript.illustrator;

/* The ink type. */
@:native("InkType") @:enum extern enum abstract InkType(Int) { 
	
	/* Cyan color ink. */
	var CYANINK;
	
	/* Magenta color ink. */
	var MAGENTAINK;
	
	/* Yellow color ink. */
	var YELLOWINK;
	
	/* Black color ink. */
	var BLACKINK;
	
	/* Custom color ink. */
	var CUSTOMINK;
	
	
}
