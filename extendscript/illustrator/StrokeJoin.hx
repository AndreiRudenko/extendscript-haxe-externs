package extendscript.illustrator;

/* The stroke joint. */
@:native("StrokeJoin") extern enum StrokeJoin {
	
	/* Mitered joints. */
	MITERENDJOIN; 

	/* Rounded joints. */
	ROUNDENDJOIN; 

	/* Beveled joints. */
	BEVELENDJOIN; 


}